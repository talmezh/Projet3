function [image] = layers2image(slice)
%Converts 3 layer OCT slice to single B/W image
dim = size(slice);
type = typename(
image = zeros(dim(1), dim(2));
for i=1:dim(3)
    image = int(image) + slice(:, : ,i);
end
end

